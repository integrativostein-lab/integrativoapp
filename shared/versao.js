/**
 * Versão canônica do Integrativo.App.
 * Manter alinhado com backend/package.json e frontend/js/config.js (CONFIG.VERSAO).
 */
module.exports = {
  /** Versão semver da aplicação (API, FHIR, deploy). */
  APP: '2.1.0',
  /** Rótulo curto para UI e documentação. */
  ROTULO: '2.1',
  /** Versão do schema de campos da anamnese (migrações e JSONB). */
  SCHEMA_ANAMNESE: '2.1'
};
